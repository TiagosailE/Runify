<<<<<<< Updated upstream
<div>
  <h1 class="font-bold text-4xl">Notifications#index</h1>
  <p>Find me in app/views/notifications/index.html.erb</p>
</div>
=======
<div class="min-h-screen bg-[#F5F5F5] dark:bg-[#1a1a1a] pb-24">
  
  <%= render 'shared/header' %>

  <main class="px-6 py-6">
    
    <div class="flex items-center justify-between mb-6">
      <h2 class="text-2xl font-bold text-gray-900 dark:text-white">Notificações</h2>
      
      <% if @unread_count > 0 %>
        <%= link_to "Marcar todas como lidas", mark_all_as_read_notifications_path, 
            data: { turbo_method: :post },
            class: "text-sm text-teal-500 hover:text-teal-600 font-medium" %>
      <% end %>
    </div>

    <% if @notifications.any? %>
      <div class="space-y-3">
        <% @notifications.each do |notification| %>
          <div class="bg-white dark:bg-[#2d3748] rounded-2xl shadow-sm overflow-hidden <%= 'border-l-4 border-teal-500' unless notification.read %>">
            <%= link_to mark_as_read_notification_path(notification), 
                data: { turbo_method: :post },
                class: "block p-4 hover:bg-gray-50 dark:hover:bg-gray-700 transition-colors" do %>
              
              <div class="flex items-start gap-4">
                <div class="flex-shrink-0 w-12 h-12 rounded-full flex items-center justify-center
                            <%= case notification.notification_type
                                when 'workout_reminder' then 'bg-blue-100 dark:bg-blue-900/30'
                                when 'sync_reminder' then 'bg-yellow-100 dark:bg-yellow-900/30'
                                when 'congratulations' then 'bg-green-100 dark:bg-green-900/30'
                                when 'weekly_summary' then 'bg-purple-100 dark:bg-purple-900/30'
                                end %>">
                  <i class="fas <%= case notification.notification_type
                                    when 'workout_reminder' then 'fa-running text-blue-500'
                                    when 'sync_reminder' then 'fa-sync text-yellow-500'
                                    when 'congratulations' then 'fa-trophy text-green-500'
                                    when 'weekly_summary' then 'fa-chart-line text-purple-500'
                                    end %> text-xl"></i>
                </div>

                <div class="flex-1 min-w-0">
                  <div class="flex items-start justify-between gap-2">
                    <h3 class="font-semibold text-gray-900 dark:text-white <%= 'font-bold' unless notification.read %>">
                      <%= notification.title %>
                    </h3>
                    <% unless notification.read %>
                      <span class="flex-shrink-0 w-2 h-2 bg-teal-500 rounded-full"></span>
                    <% end %>
                  </div>
                  
                  <p class="text-sm text-gray-600 dark:text-gray-400 mt-1">
                    <%= notification.message %>
                  </p>
                  
                  <p class="text-xs text-gray-400 dark:text-gray-500 mt-2">
                    <%= time_ago_in_words(notification.sent_at) %> atrás
                  </p>
                </div>
              </div>
            <% end %>
          </div>
        <% end %>
      </div>

      <% if @notifications.count > 20 %>
        <div class="mt-6 text-center">
          <p class="text-sm text-gray-500 dark:text-gray-400">
            Mostrando as últimas notificações
          </p>
        </div>
      <% end %>
    <% else %>
      <div class="bg-white dark:bg-[#2d3748] rounded-2xl shadow-sm p-12 text-center">
        <div class="w-20 h-20 bg-gray-100 dark:bg-gray-700 rounded-full flex items-center justify-center mx-auto mb-4">
          <i class="fas fa-bell-slash text-3xl text-gray-400"></i>
        </div>
        <h3 class="text-lg font-semibold text-gray-900 dark:text-white mb-2">Nenhuma notificação</h3>
        <p class="text-gray-500 dark:text-gray-400">Você não tem notificações no momento</p>
      </div>
    <% end %>

  </main>

  <%= render 'shared/footer' %>

</div>
>>>>>>> Stashed changes
